/** "Class" for Section; used for Scenario Business/Technical sections and copied from the C# class of the same name. */
import { SectionItem } from "./SectionItem";
export interface Section {
	 Items: Array<SectionItem>;
}